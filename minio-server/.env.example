# MinIO Configuration
# Rename this file to .env on the NAS and update the credentials

# Root username for MinIO (min 3 characters)
MINIO_ROOT_USER=admin

# Root password for MinIO (min 8 characters)
MINIO_ROOT_PASSWORD=SuperSecretPassword2026!
